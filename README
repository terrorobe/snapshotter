Installation
============

Requirements:

*) Perl 5.8 or later
*) Linux::LVM
*) Sys::Filesystem

On Debian/Lenny:

apt-get install liblinux-lvm-perl dh-make-perl
dh-make-perl --build --install --CPAN Sys::Filesystem
